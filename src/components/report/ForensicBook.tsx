"use client";

/**
 * LEGACY — NON-PRODUCTION. Do not import from any report renderer.
 *
 * The previous 24-page forensic book fabricated leak rates, COMPLIANT statuses,
 * fallback defect/size codes, steward names, CAPA records, signatories,
 * automatic escalation, and unconditional completeness claims. Those values
 * must not enter a report view model or generated output.
 *
 * Replacement: Financial Year Audit Pack (`src/lib/report/report-builders.ts`).
 */

export default function ForensicBook(): never {
  throw new Error(
    "ForensicBook is quarantined non-production legacy code and must not be rendered.",
  );
}
